[package]
name = "metricsrbx/election-system"
version = "0.1.0"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"

[dev-dependencies]
Fusion = "elttob/fusion@0.3.0"
Iris = "sirmallard/iris@2.5.1"
ProfileService = "firebird702/profileservice@1.1.0"
Cmdr = "evaera/cmdr@1.11.0"
TestEZ = "roblox/testez@0.4.1"

# After changing dependencies: run `wally install` and sync DevPackages → ReplicatedStorage.Packages via Rojo (see default.project.json Packages → DevPackages).